extends@: default

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        content:
          type: tab
          title: "Studio"

          fields:

            header.media_order:
              unset@: true

            header.english:
              type: editor
              label: "English version"
